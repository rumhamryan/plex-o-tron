TODO:

- Do not allow for selection of tv episodes that have yet to be released
    - For both single episodes and entire seasons
- Ignore all input that is not a link, or one of the commands until a command is sent
- Star Wars: Visions does not resolve to a wikipedia page, debug and fix
- There is a bug where if a file has two version (1080p and 4k), deleting one seems to delete both